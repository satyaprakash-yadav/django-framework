#               GITHUB HOST COMMAND

# 1). cmd >>> git init

# 2). cmd >>> git status

# 3). cmd >>> git add .

# 4). cmd >>> git commit -m "first commit"

# 5). cmd >>> git branch -M main (if we not select this command then default branch is master bnch)

# 6). cmd >>> git remote add origin https://github.com/

# 7). cmd >>> git push -u origin master



#               GITHUB PUSH

# Change in existing folder or file :-

# 1). cmd >>> git status

# 2). cmd >>> git add .

# 3). cmd >>> git commit -m "some desc"

# 4). cmd >>> git push
